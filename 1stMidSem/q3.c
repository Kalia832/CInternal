#include<stdio.h>
int main()
{
    int arr[5]={12,13};
    printf("%d %d %d",arr[2],arr[3],arr[4]);
    return 0;
    //from this we learn if we initialize half veriables at the time of declaration ,the uninitialized variables are became zero(0);
}